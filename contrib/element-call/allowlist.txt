# Element Call specs Spindle must pass -- the ratchet over upstream's own
# Playwright suite (#269), enforced by scripts/element-call-check.py the
# way complement/allowlist.txt is by complement-check.py.
#
# One entry per line: `<spec file> :: <test title>`, as the checker prints
# them. A test that passes and is not here is printed as a candidate;
# adding it is a reviewed promotion, never automatic, so a flaky spec cannot
# teach everyone to ignore this gate.
#
# Nothing is claimed that has not been seen to pass: every entry names the
# run that recorded it.
#
# Recorded on the first run (workflow run 34014884679, element-call pin
# a03f23e, chromium): four of four, including a two-party call with video
# tiles on both sides -- LiveKit, lk-jwt-service and Spindle's transport
# discovery composing, which is the composition #41 says no unit test
# reaches.
access.spec.ts :: As a guest, create a call, share link and other join
access.spec.ts :: Sign up a new account, then login, then logout
landing.spec.ts :: Landing page
landing.spec.ts :: has title
